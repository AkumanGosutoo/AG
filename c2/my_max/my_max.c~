int     my_max(int itab[], int len)
{
  int	count;
  int	nb;

  count = 1;
  nb = itab[0];
  while (count < len)
    {
      if (itab[count] > nb)
	nb = itab[count];
      count++;
    }
  return (nb);
}
